<%= form_with(model: reserva) do |form| %>
  <% if reserva.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reserva.errors.count, "error") %> prohibited this reserva from being saved:</h2>

      <ul>
        <% reserva.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :cliente_id, style: "display: block" %>
    <%= form.collection_select :cliente_id, Cliente.all, :id, :nome_completo %>
  </div>

  <div>
    <%= form.label :quarto, style: "display: block" %>
    <%= form.number_field :quarto %>
  </div>

  <div>
    <%= form.label :entrada, style: "display: block" %>
    <%= form.date_field :entrada %>
  </div>

  <div>
    <%= form.label :saida, style: "display: block" %>
    <%= form.date_field :saida %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
